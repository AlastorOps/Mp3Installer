TELEGRAM_BOT_TOKEN=
FFMPEG_LOCATION=
YT_COOKIES_FILE=
# Your Render service's public URL (Render also auto-sets RENDER_EXTERNAL_URL,
# so this is only needed for local testing or to override it).
WEBHOOK_URL=
